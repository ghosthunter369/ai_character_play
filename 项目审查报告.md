# AI Character Play 项目审查报告

## 审查范围
- 后端：Spring Boot / MyBatis / WebSocket / LangChain4j 核心接口与配置。
- 安全：鉴权、越权、CORS、敏感信息管理。
- 可维护性：接口设计一致性与配置可迁移性。

## 结论摘要
- 项目整体结构清晰，前后端分层明确，具备可运行的 MVP 能力。
- 发现并修复了 4 类高优先级问题：
  1. 配置文件中存在明文敏感信息。
  2. CORS 使用 `allowCredentials(true)` + `allowedOriginPatterns("*")` 存在风险。
  3. 用户详情 VO 接口误复用管理员接口导致普通用户无法访问。
  4. 导出聊天记录接口存在越权风险（可通过传入 `userId` 导出他人数据）。

## 已完成整改

### 1) 敏感配置改为环境变量注入
- 将数据库密码、DashScope API Key、讯飞凭据等改为 `${ENV_VAR:}` 形式。
- 增加本地默认值仅用于开发（如 `DB_URL` 默认 localhost），避免硬编码线上地址。

### 2) CORS 安全收敛
- 增加 `app.cors.allowed-origin-patterns` 配置项。
- 默认仅允许 `http://localhost:*`，并支持通过环境变量覆盖。

### 3) 用户 VO 查询接口修正
- `GET /user/get/vo` 不再调用管理员权限接口，而是直接按 ID 查询并返回脱敏对象。
- 这样普通登录用户也可按预期获取公开 VO 信息。

### 4) 聊天记录导出权限修正
- `GET /chatHistory/exportChatHistoryTxt` 新增登录态校验。
- 默认只导出当前登录用户数据。
- 仅当调用者为管理员时，才允许导出指定 `userId` 数据。

## 建议后续优化（未在本次提交中改动）
1. **密码哈希升级**：`MD5 + 固定盐` 建议升级为 `BCrypt/Argon2`。
2. **接口限流**：对登录、聊天、导出接口增加限流与风控。
3. **审计日志**：记录导出行为（操作人、目标 userId、时间、IP）。
4. **依赖治理**：部分依赖为 beta 版本，建议锁定稳定版本并做 SCA 扫描。

## 回归建议
- 重点回归：
  - 普通用户访问 `/user/get/vo` 是否正常。
  - 普通用户导出他人聊天记录是否被拒绝。
  - 管理员导出指定 `userId` 是否仍可用。
  - 前端跨域请求在开发环境（localhost）是否正常。
